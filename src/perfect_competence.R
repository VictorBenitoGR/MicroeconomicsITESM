# Utilidades económicas

# * Utilidad económicas (pi) = IT - CT
# Donde IT = P * Q
# Donde CT = CF + CV + costo de oportunidad

# * Ingreso Marginal (IMg):
# Ingreso adicional obtenido por cada unidad adicional que se vende
# ΔIT / ΔQ = IT / Q

# * Ingreso Medio (IMe):
# Ingreso promedio obtenido por cada unidad vendida
# IT / Q = (P * Q) / Q

# Queremos producir cuando IMg = CMg

# * Utilidad económicas (pi) = IT - CT
#                          = (P * Q) - (CF + CV)  + costo de oportunidad
#                          = (P * Q) - (CFMe * Q + CVMe * Q) = P * Q - CFMe * Q - CVMe * Q #nolint
# * Beneficio económico (pi) = (P - CTMe)Q

# * Maximizar ganancias
# IMG Vs CMg

# * Ganancias (+, - o 0)
# P Vs CTMe

# * Punto de cierre
# P Vs CVMe

# Cuando CMg es menor a CVMe, se cierra la producción
# Q cuando P = CMg
# IT / Q = (P * Q) / Q
# Donde CT = CTMe * Q
# pi = IT - CT

p = 2
q = 2
ct_me = 2
it = p * q
ct = ct_me * q
be_pi = it - ct

it
ct
be_pi

# Moonopolio
# Va a producir cuando
IMg = CMg

q = 
p = 
ct_me = 
it = p * q
CT = ct_me * q
be_pi = it - CT

it
ct
be_pi

CMg = costo_social
Excedente_consumidor = x # Mitad inferior curva de demanda
Excedente_productor = x # Mitad superior curva de demanda